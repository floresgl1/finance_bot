# --- Execution ---------------------------------------------------------
# Declared explicitly: live_trader.py previously relied on a runtime
# subprocess pip-install fallback if this import failed.
alpaca-trade-api

# --- Data / ML ---------------------------------------------------------
yfinance
pandas
numpy
scikit-learn
ta
xgboost
shap

# --- Sentiment (FinBERT) ----------------------------------------------
transformers
torch

# --- Agent -------------------------------------------------------------
tavily-python
groq
tenacity

# --- Misc --------------------------------------------------------------
python-dotenv
requests
